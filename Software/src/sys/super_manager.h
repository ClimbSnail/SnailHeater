#ifndef SUPER_MANAGER_H
#define SUPER_MANAGER_H

class SuperManager
{
private:

public:
    SuperManager();
    ~SuperManager();
    bool initController();
    bool loadWindows();
    bool restart();
    
};

#endif